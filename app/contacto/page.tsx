import { ContactPageClient } from './ContactPageClient';

export const metadata = {
    title: 'Contacto',
    description: 'Contáctanos por WhatsApp, email o teléfono para pedidos, dudas o cotizaciones de mayoreo. Estamos para ayudarte.',
};

export default function ContactoPage() {
    return <ContactPageClient />;
}
